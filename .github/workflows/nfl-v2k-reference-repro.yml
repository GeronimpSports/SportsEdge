name: nfl-v2k-reference-repro

on:
  pull_request:
    paths:
      - '.github/workflows/nfl-v2k-reference-repro.yml'
      - 'scripts/build_nfl_v2k_empirical_key_reference.py'
      - 'sportsedge/sports/nfl/NFL_V2K_CLEAN_PREREG_2026-09-13.md'
      - 'sportsedge/sports/nfl/NFL_V2K_EMPIRICAL_KEY_REFERENCE_V1.json'
      - 'tests/test_nfl_v2k_empirical_key_reference.py'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  reproduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - name: Download frozen evidence artifact
        env:
          GH_TOKEN: ${{ github.token }}
          ARTIFACT_ID: '10319922606'
          EXPECTED_ARTIFACT_SHA256: 'e53f16804c74ec197b23dd17180ad88d1c696d4a93e593ecc17fa8ccf480a437'
        run: |
          set -euo pipefail
          mkdir -p /tmp/nfl-v2k-evidence
          curl --fail-with-body --location \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/${ARTIFACT_ID}/zip" \
            --output /tmp/nfl-v2k-evidence.zip
          printf '%s  %s\n' "${EXPECTED_ARTIFACT_SHA256}" /tmp/nfl-v2k-evidence.zip | sha256sum --check --strict
          unzip -q /tmp/nfl-v2k-evidence.zip -d /tmp/nfl-v2k-evidence
      - name: Rebuild frozen signed-key reference
        run: |
          set -euo pipefail
          python scripts/build_nfl_v2k_empirical_key_reference.py \
            --template sportsedge/sports/nfl/NFL_V2K_EMPIRICAL_KEY_REFERENCE_V1.json \
            --games /tmp/nfl-v2k-evidence/sources/games.csv \
            --manifest /tmp/nfl-v2k-evidence/nfl_source_manifest.json \
            --production-validation /tmp/nfl-v2k-evidence/nfl_production_validation.json \
            --promotion-registry /tmp/nfl-v2k-evidence/nfl_promotion_registry.json \
            --output /tmp/NFL_V2K_EMPIRICAL_KEY_REFERENCE_V1.rebuilt.json
      - name: Prove byte-for-byte reproduction
        run: |
          set -euo pipefail
          cmp --silent \
            sportsedge/sports/nfl/NFL_V2K_EMPIRICAL_KEY_REFERENCE_V1.json \
            /tmp/NFL_V2K_EMPIRICAL_KEY_REFERENCE_V1.rebuilt.json
          sha256sum \
            sportsedge/sports/nfl/NFL_V2K_EMPIRICAL_KEY_REFERENCE_V1.json \
            /tmp/NFL_V2K_EMPIRICAL_KEY_REFERENCE_V1.rebuilt.json
